请基于以下内容，生成一版适合发布到 X thread 的中文拆帖稿。

视频标题：{{ video_title }}

要求：
- 直接输出 thread 正文，不要写前言、后记、说明文字。
- 输出 6 到 10 条短帖。
- 每条单独成段，使用 `1.`、`2.`、`3.` 这种编号。
- 第一条必须足够有抓力，像真正的首帖，而不是背景介绍。
- 每条尽量短，适合 X 的阅读和转发节奏。
- 相邻帖子之间要有推进关系，不要只是把笔记拆碎。
- 不虚构事实，不添加原视频没有的信息。
- 表达可以有传播感，但不要夸张失真。
- 最后一条负责收束，可以是判断、趋势、启发或讨论点。

禁止项：
- 不要输出“下面是 thread”
- 不要加表情雨
- 不要在结尾附“每条可单独发布”这类说明
- 不要加多余标签，除非素材里非常自然地需要

素材：
{{ transcript }}
